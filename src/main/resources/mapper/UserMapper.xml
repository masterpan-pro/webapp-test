<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.demo.dao.UserMapper">
    <!--auto generated Code-->
    <resultMap id="BaseResultMap" type="com.demo.entity.User">
        <result column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="birthday" property="birthday" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="Base_Column_List">
        id,
        user_name,
        password,
        birthday
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO user (
        id,
        user_name,
        password,
        birthday
        ) VALUES (
        #{user.id,jdbcType=BIGINT},
        #{user.userName,jdbcType=VARCHAR},
        #{user.password,jdbcType=VARCHAR},
        #{user.birthday,jdbcType=TIMESTAMP}
        )
    </insert>

    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="user.id!=null">id,</if>
            <if test="user.userName!=null">user_name,</if>
            <if test="user.password!=null">password,</if>
            <if test="user.birthday!=null">birthday,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="user.id!=null">#{user.id,jdbcType=BIGINT},
            </if>
            <if test="user.userName!=null">#{user.userName,jdbcType=VARCHAR},
            </if>
            <if test="user.password!=null">#{user.password,jdbcType=VARCHAR},
            </if>
            <if test="user.birthday!=null">#{user.birthday,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO user (
        <include refid="Base_Column_List"/>
        )VALUES
        <foreach collection="users" item="user" index="index" separator=",">
            (
            #{user.id,jdbcType=BIGINT},
            #{user.userName,jdbcType=VARCHAR},
            #{user.password,jdbcType=VARCHAR},
            #{user.birthday,jdbcType=TIMESTAMP}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE user
        <set>
            <if test="user.id != null">id= #{user.id,jdbcType=BIGINT},</if>
            <if test="user.userName != null">user_name= #{user.userName,jdbcType=VARCHAR},</if>
            <if test="user.password != null">password= #{user.password,jdbcType=VARCHAR},</if>
            <if test="user.birthday != null">birthday= #{user.birthday,jdbcType=TIMESTAMP}</if>
        </set>
        WHERE id = #{user.id,jdbcType=BIGINT}
    </update>

    <!--auto generated by codehelper on 2018-05-24 14:50:33-->
    <select id="find" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user
    </select>

    <select id="getUserValueMap" resultType="java.util.Map">
        select
        <include refid="Base_Column_List"/>
        from user
    </select>
    
    <select id="getUserInfoMap" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user
    </select>
</mapper>

